<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.manddprojectconsulant.stagnonew">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:requestLegacyExternalStorage="true"
        android:theme="@style/MyTheme">
        <activity android:name="com.manddprojectconsulant.stagnonew.MainActivity">

        </activity>


        <activity android:name="com.manddprojectconsulant.stagnonew.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <provider
            android:name="com.manddprojectconsulant.stagnonew.GenericFileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
        <activity
            android:name="com.manddprojectconsulant.stagnonew.EncryptionActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name="com.manddprojectconsulant.stagnonew.ExtractionActivity" />
        <activity android:name="com.manddprojectconsulant.stagnonew.AskActivity"></activity>
    </application>

</manifest>